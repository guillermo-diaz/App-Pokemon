import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    card: {
        borderWidth: 1,
        borderColor: "grey",
        borderRadius: 8,
        padding: 24,
    },
});

export default styles; 